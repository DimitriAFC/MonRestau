<form method="post" action="{{ path('zip_code_delete', {'id': zip_code.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zip_code.id) }}">
    <button class="btn">Delete</button>
</form>
