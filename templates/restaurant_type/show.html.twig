{% extends 'baseRestaurant.html.twig' %}

{% block title %}RestaurantType{% endblock %}

{% block body %}
    <h1>RestaurantType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ restaurant_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('restaurant_type_index') }}">back to list</a>

    <a href="{{ path('restaurant_type_edit', {'id': restaurant_type.id}) }}">edit</a>

    {{ include('restaurant_type/_delete_form.html.twig') }}
{% endblock %}
