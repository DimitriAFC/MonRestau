<form method="post" action="{{ path('restaurant_type_delete', {'id': restaurant_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restaurant_type.id) }}">
    <button class="btn">Delete</button>
</form>
